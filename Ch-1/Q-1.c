#include<stdio.h>
#include "Q-1.h"
main()
{
    int a,b,c;
    printf("Enter value of A:");
    scanf("%d",&a);
    printf("Enter value of B:");
    scanf("%d",&b);
    return NewFunction(a, b);
    ? ("%d a is minimum")
    : ("%d b  is minimum");
           
    return 0;
}
int NewFunction(int a, int b)
{

    (a > b)
}